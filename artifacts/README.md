# Artifacts

- `vX.Y.Z/` contains a versioned bot release and its runtime weights.
- `temp/` contains ignored local training runs and failed experiments.

Training and evaluation commands must write below this directory.
